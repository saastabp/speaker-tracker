# Production build (`vite build` / `--mode production`): real Cognito OIDC. The Cognito authority
# and client id are NOT build-time values — they come from /config.json, which the CDK Frontend
# stack writes from the prod-Auth outputs at deploy time (single source of truth = the stack).
VITE_AUTH_MODE=cognito